import{c as t,Q as s,j as o,m as a}from"./chunks/framework.BPKcPtvA.js";const u=JSON.parse(`{"title":"Security overview","description":"A summary of GateKeeper's security model and the threats it addresses.","frontmatter":{"title":"Security overview","description":"A summary of GateKeeper's security model and the threats it addresses."},"headers":[],"relativePath":"src/content/docs/security/overview.md","filePath":"src/content/docs/security/overview.md"}`),r={name:"src/content/docs/security/overview.md"};function n(i,e,c,d,p,h){return s(),o("div",null,[...e[0]||(e[0]=[a('<p>GateKeeper is designed to be a trustworthy front door for self-hosted services. This page explains the security model and the threats it is designed to address.</p><h2 id="what-gatekeeper-protects-against" tabindex="-1">What GateKeeper protects against <a class="header-anchor" href="#what-gatekeeper-protects-against" aria-label="Permalink to &quot;What GateKeeper protects against&quot;">​</a></h2><p><strong>Stolen passwords.</strong> Passwords alone are not enough. GateKeeper requires a second factor (email OTP, TOTP, or passkey) for every login. Even if a user&#39;s password is leaked, an attacker cannot log in without also compromising their email, phone, or device.</p><p><strong>Brute-force attacks on login.</strong> After 20 failed login attempts from the same IP address within 15 minutes, all further attempts from that IP are rejected until the window resets. This applies before the password check, so it slows automated credential-stuffing regardless of whether the email is valid.</p><p><strong>Brute-force attacks on OTP and TOTP.</strong> OTP and TOTP codes lock the account after 5 failed attempts within 10 minutes. Accounts unlock automatically after 10 minutes.</p><p><strong>Slow password cracking.</strong> Password hashing uses argon2id with parameters that make each verification take roughly 100-200ms on typical hardware. Even with a full database dump, recovering passwords at scale is not practical.</p><p><strong>Session theft.</strong> Sessions are stored server-side. The cookie only contains an opaque random token. If someone reads your cookie, they can impersonate you until the session expires or is revoked - but they cannot extract other credentials from it.</p><p><strong>Cross-site request forgery (CSRF).</strong> Every state-changing POST request - password change, 2FA enrollment, session revocation, passkey deletion - requires a CSRF token that matches a value set in a cookie. Form submissions from other sites do not carry this token and are rejected.</p><p><strong>Database exposure of OTP codes.</strong> Email OTP codes are stored as HMAC-SHA256 digests keyed with the application secret, not as plaintext. A database dump without the secret key cannot be used to reconstruct or brute-force active codes.</p><p><strong>Database exposure of TOTP secrets.</strong> TOTP secrets are encrypted with AES-256-GCM before being stored. The encryption key is derived from <code>SECRET_KEY</code> using SHA-256. A database dump without the key cannot be used to clone authenticator apps.</p><p><strong>Password reset abuse.</strong> Reset tokens are 32 bytes of random data, hashed with argon2id before storage. They expire in 30 minutes and are single-use. Rate limits prevent mass generation of tokens.</p><p><strong>Link preview bots.</strong> Some email clients fetch links automatically to generate previews. GateKeeper&#39;s reset tokens are only consumed on a successful POST, not on GET, so a preview bot cannot invalidate a token before the user clicks it.</p><p><strong>Phishing (passkeys).</strong> Passkeys are bound to the specific origin they were created for. A passkey created at <code>auth.example.com</code> will not work on a lookalike site.</p><h2 id="what-gatekeeper-does-not-protect-against" tabindex="-1">What GateKeeper does not protect against <a class="header-anchor" href="#what-gatekeeper-does-not-protect-against" aria-label="Permalink to &quot;What GateKeeper does not protect against&quot;">​</a></h2><p><strong>Compromised server.</strong> If an attacker has shell access to the host, they can read the database, the <code>SECRET_KEY</code>, and the session cookies directly. Keep the host secure.</p><p><strong>Email compromise.</strong> Both email OTP and password recovery rely on the user&#39;s email inbox. A compromised inbox gives an attacker a path in.</p><p><strong>Compromise of SECRET_KEY.</strong> TOTP secrets and OTP HMACs are keyed with <code>SECRET_KEY</code>. If the key is compromised, rotate it immediately and revoke all sessions. Users will need to re-enroll TOTP since the encrypted secrets in the database cannot be decrypted with the new key.</p><h2 id="defense-in-depth" tabindex="-1">Defense in depth <a class="header-anchor" href="#defense-in-depth" aria-label="Permalink to &quot;Defense in depth&quot;">​</a></h2><p>No single security control is perfect. GateKeeper layers multiple controls - strong password hashing, HMAC-keyed OTP storage, AES-GCM TOTP encryption, short-lived OTPs, brute-force lockouts, IP-based login rate limiting, secure cookies, CSRF protection, security headers, and audit logging - so that defeating one control is not enough to compromise the system.</p>',19)])])}const g=t(r,[["render",n]]);export{u as __pageData,g as default};
